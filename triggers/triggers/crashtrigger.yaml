match: "crash"
interaction: triggers/crash

channels:
  blacklist:
    - "${ CHANNELS.WELCOME }"
    - "${ CHANNELS.SOURCE_STATUS }"
    - "${ CHANNELS.RULES }"
    - "${ CHANNELS.ANNOUNCEMENTS }"
    - "${ CHANNELS.RELEASES }"
    - "${ CHANNELS.EXTERNAL_REPOS }"
    - "${ CHANNELS.CHAT }"
    - "${ CHANNELS.GAME_DISCUSSIONS }"
    - "${ CHANNELS.MIHOYO_TALK }"
    - "${ CHANNELS.MANGA_DISCUSSIONS }"
    - "${ CHANNELS.ANIME_DISCUSSIONS }"
    - "${ CHANNELS.ANIMEMES }"
    - "${ CHANNELS.BOT_COMMANDS }"
    - "${ CHANNELS.VOICE_CHAT }"
    - "${ CHANNELS.RND }"
    - "${ CHANNELS.MASTER_RND }"
    - "${ CHANNELS.ALPHA_CHAT }"
    - "${ CHANNELS.EXT_DEV }"
    - "${ CHANNELS.WEBSITE }"
