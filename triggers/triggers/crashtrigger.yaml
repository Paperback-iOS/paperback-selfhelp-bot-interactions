match: "crash"
interaction: triggers/crash

channels:
  whitelist:
    - "${ CHANNELS.SUPPORT }"
  blacklist:
    - "${ CHANNELS.WELCOME }"
    - "${ CHANNELS.RULES }"
    - "${ CHANNELS.ANNOUNCEMENTS }"
    - "${ CHANNELS.RELEASES }"
    - "${ CHANNELS.ALPHA_CHAT }"