match: "ERROR INSTANCE ID"
interaction: triggers/errorinstanceid

channels:
  whitelist:
    - "${ CHANNELS.SUPPORT }"
  blacklist:
    - "${ CHANNELS.WELCOME }"
    - "${ CHANNELS.RULES }"
    - "${ CHANNELS.ANNOUNCEMENTS }"
    - "${ CHANNELS.RELEASES }"
    - "${ CHANNELS.ALPHA_CHAT }"